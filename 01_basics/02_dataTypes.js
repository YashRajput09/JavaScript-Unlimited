// datatype is used to define the type of data. 
// In JS, no need to define the type of data, js automatically detect

let Name = "Yash"; //string,
let age = 18;      //number,
let isLoggedIn = true;//boolean,
let money = null;   // null, is object in js.
let hobbies; //undefine, because  not assigned any value to it.
let bigInt1 = 1234567890123456789012345678901234567890n; //bigint

// typeof operator is used to check the type of data
console.log(typeof Name);
console.log(typeof age);
console.log(typeof isLoggedIn);
console.log(typeof money);
console.log(typeof hobbies);
console.log(typeof bigInt1);